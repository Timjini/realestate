<!-- Product features -->
 <% @properties.each do |property| %>

    <%= image_tag "http://localhost:3000/#{property.photo}"%>
    
            <%= property.city%> <%= property.country%>

     
    <% end %>
   